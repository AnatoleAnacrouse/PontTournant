name: Pont tournant
  uses: wokwi/wokwi-ci-action@v1
  with:
    token: ${{ secrets.WOKWI_PONT_TOURNANT_TOKEN }}
    path: / # directory with wokwi.toml, relative to repo's root
    timeout: 10000
    expect_text: 'Hello'
    fail_text: 'Error'
    serial_log_file: 'log.txt' # optional
